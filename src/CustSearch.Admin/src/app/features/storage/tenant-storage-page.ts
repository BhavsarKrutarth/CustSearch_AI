import {ChangeDetectionStrategy,Component,inject,OnInit,signal} from '@angular/core';
import {AdminShell} from '../../shared/admin-shell/admin-shell';
import {StorageApiService,StorageSummary} from './storage-api.service';
@Component({selector:'app-tenant-storage-page',imports:[AdminShell],changeDetection:ChangeDetectionStrategy.OnPush,template:`<app-admin-shell eyebrow="Tenant / Evidence" pageTitle="Camera evidence storage">@if(error()){<p class="error" role="alert">{{error()}}</p>}@if(value();as s){<section class="hero"><div><b>{{gb(s.usage.usedBytes)}} GB / {{gb(s.usage.quotaBytes)}} GB</b><span>{{s.usage.usagePercent}}% used · {{s.usage.pressureLevel}}</span></div><progress [value]="s.usage.usedBytes" [max]="s.usage.quotaBytes"></progress><p>{{gb(s.usage.availableBytes)}} GB available</p></section><section class="grid"><article><b>{{mb(s.usage.snapshotBytes)}} MB</b><span>Snapshots</span></article><article><b>{{mb(s.usage.motionClipBytes)}} MB</b><span>Motion clips</span></article><article><b>{{mb(s.usage.securityEvidenceBytes)}} MB</b><span>Security evidence</span></article><article><b>{{s.policy.defaultRetentionDays}} days</b><span>Default retention</span></article><article><b>{{s.policy.autoCleanupEnabled?'Enabled':'Disabled'}}</b><span>Auto cleanup</span></article><article><b>{{s.policy.storageEnabled?'Enabled':'Disabled'}}</b><span>Evidence capture</span></article></section><p class="boundary">Live monitoring continues even when evidence storage is full. Storage contains event snapshots and short clips, not continuous recording.</p>}@else if(!error()){<p>Loading storage usage…</p>}</app-admin-shell>`,styles:[`.hero,.grid article,.boundary{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-sm);padding:1rem}.hero div,.grid article{display:grid;gap:.3rem}.hero b{font-size:1.7rem}.hero progress{width:100%;margin-top:1rem}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem;margin:1rem 0}.grid b{font-size:1.3rem}.grid span,.hero span{color:var(--color-muted)}.boundary{border-left:4px solid var(--color-accent)}.error{color:var(--color-danger)}@media(max-width:800px){.grid{grid-template-columns:1fr}}`],})export class TenantStoragePage implements OnInit{private readonly api=inject(StorageApiService);protected readonly value=signal<StorageSummary|null>(null);protected readonly error=signal('');ngOnInit():void{this.api.tenant().subscribe({next:x=>this.value.set(x),error:()=>this.error.set('Storage usage could not be loaded.')});}protected gb(x:number):string{return(x/1073741824).toFixed(2);}protected mb(x:number):string{return(x/1048576).toFixed(1);}}
