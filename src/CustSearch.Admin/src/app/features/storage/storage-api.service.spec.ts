import {provideHttpClient} from '@angular/common/http';
import {HttpTestingController,provideHttpClientTesting} from '@angular/common/http/testing';
import {TestBed} from '@angular/core/testing';
import {StorageApiService} from './storage-api.service';
describe('StorageApiService',()=>{let service:StorageApiService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(StorageApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('loads tenant storage without browser TenantId',()=>{service.tenant().subscribe();const r=http.expectOne('/api/tenant/storage');expect(r.request.method).toBe('GET');expect(r.request.url).not.toContain('tenantId');r.flush({});});it('uses an explicit route-scoped tenant only for platform policy management',()=>{service.savePlatform(42,{storageEnabled:true,storageQuotaBytes:2147483648,defaultRetentionDays:15,motionSnapshotRetentionDays:15,motionClipRetentionDays:15,falsePositiveRetentionDays:3,unreviewedEvidenceRetentionDays:15,confirmedIncidentRetentionDays:30,warningPercent:80,criticalPercent:90,allowSnapshots:true,allowMotionClips:true,autoCleanupEnabled:true,quotaPressurePolicy:1}).subscribe();const r=http.expectOne('/api/platform/tenants/42/storage/policy');expect(r.request.method).toBe('PUT');expect(r.request.body.tenantId).toBeUndefined();r.flush({});});});
