import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{OperationsApiService}from'./operations-api.service';describe('OperationsApiService',()=>{let api:OperationsApiService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});api=TestBed.inject(OperationsApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('uses fixed platform routes and never accepts tenant injection',()=>{api.pause('exports','maintenance').subscribe();const request=http.expectOne('/api/platform/operations/workers/exports/pause');expect(JSON.stringify(request.request.body)).not.toContain('tenantId');request.flush({workerType:'exports',isPaused:true});});it('loads masked references without a secret endpoint',()=>{api.secrets().subscribe();const request=http.expectOne('/api/platform/operations/secret-references');expect(request.request.method).toBe('GET');request.flush([{maskedReference:'****abcd'}]);});});
