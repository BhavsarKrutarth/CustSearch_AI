[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]
testpaths = ["../../tests/CustSearch.AI.Tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
